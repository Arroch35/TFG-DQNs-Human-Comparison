[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "tfg-dqn-human"
version = "0.1.0"

[tool.setuptools.packages.find]
where = ["."]
include = ["src*"]